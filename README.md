# shortlist

[![Open in Bolt](https://bolt.new/static/open-in-bolt.svg)](https://bolt.new/~/sb1-lvzvtksr)

## Local setup

1. Install deps:
   - `npm install`
2. Create `.env.local` from `.env.example` and set:
   - `MONGODB_URI=...`
3. Run:
   - `npm run dev`
